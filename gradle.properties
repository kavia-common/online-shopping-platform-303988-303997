# Root-level Gradle properties (safe defaults).
# This file exists to support CI environments that expect Gradle config in the repository root.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
